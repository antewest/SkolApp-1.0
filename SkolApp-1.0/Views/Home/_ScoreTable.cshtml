<div class="row">
    <h2 class="header">Poäng:</h2>
    <div class="col-xs-12">
        <table class="col-sm-6 col-xs-12">
            <thead>
                <tr class="col-xs-12">
                    <th class="col-xs-4">
                        Plats
                    </th>
                    <th class="col-xs-4">
                        NickName
                    </th>
                    <th class="col-xs-4">
                        Poäng
                    </th>
                </tr>
            </thead>
            <tbody>
                <tr ng-repeat="(key, val) in scores" class="col-xs-12">
                    <td class="col-xs-4">{{key+1}}</td>
                    <td class="col-xs-4">{{val.NickName}}</td>
                    <td class="col-xs-4">{{val.Points}}</td>
                </tr>
            </tbody>
        </table>
    </div>
</div>